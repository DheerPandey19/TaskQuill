<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TaskQuill</title>
    <script src="https://cdn.tailwindcss.com"></script>
   
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 border-t-4 border-blue-600">
        <div class="flex items-center justify-center h-70">
            <h1 style="font-size: 3rem; font-weight: 550; font-family: 'Montserrat', sans-serif; color: #ffffff; letter-spacing: 1px;">
                TaskQuill
            </h1>
        </div>

        <div class="px-10 py-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full px-5 py-3 bg-zinc-800 outline-none rounded-md" type="text" placeholder="Title Goes Here" name="title" >
                <textarea class="block w-full px-5 py-3 mt-2 resize-none bg-zinc-800 outline-none rounded-md" placeholder="Write your task here" name="details"></textarea>

    
                <input class="mt-3 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create New Task">
            </form>
        </div>
        <div class="tasksholder p-10 flex gap-4 flex-wrap">

            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task  m-w-72 px-6 py-6 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter">
                            <%=val%>
                        </h1>
                        <div class ="w-full flex mt-3 gap-3 items-center justify-between">
                            <a class="text-blue-500 inline-block " href="/file/<%=val %>">Read Task</a>

                            <form action="/delete/<%= val %>" method="POST">
                                <button type="submit" class="text-green-500 inline-block">Done</button>
                            </form>
                        </div>
                        
                    </div>
                <% }) %>
                <%}else{ %>
                    <h3 class="text-zinc-400"> No Pending Tasks</h3>
                <% } %>
        </div>
    </div>

    
</body>
</html>